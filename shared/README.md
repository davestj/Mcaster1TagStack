# shared/ — cross-process types + JSON schemas

Plain C++17 headers usable by desktop, daemon, and any tool. No Qt,
no MFC, no platform headers. Types here define the wire format that
the JSON API contract serializes to.

## Status

Phase 1: MediaItem, Icy2Fields, plus the API envelope schema.
