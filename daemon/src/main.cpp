// main.cpp — Mcaster1TagStackd entry point (Phase 1 stub).
// Phase 2 will replace this with the real HTTPD + scheduler + API.

#include <iostream>

int main(int argc, char** argv) {
    std::cout << "tagstackd v0.1.0 — Phase 1 skeleton\n"
              << "Phase 2 will replace this with the real daemon.\n";
    return 0;
}
