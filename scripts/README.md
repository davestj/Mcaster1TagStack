# scripts/ — build, test, deploy helpers

Conventions: bash where portable, Python where complex. All scripts
self-documenting with `--help`.

## Status

Phase 1: empty. Phase 2 adds `build-daemon.sh`, `build-desktop.sh`,
`run-tests.sh`, `migrate.sh`.
